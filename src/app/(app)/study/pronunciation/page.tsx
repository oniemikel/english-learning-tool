import { StudySession } from '@/components/study/study-session';

export default function StudyPronunciationPage() {
  return <StudySession title="発音学習" promptLabel="発音する単語" promptValue="allocate" answerValue="認識結果: allocate" />;
}
